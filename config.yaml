data:
  raw_dir: "data/raw"
  processed_dir: "data/processed"

twitch:
  clips:
    days_back: 30
    max_per_streamer: 100
  streamers:
    - name: "stableronaldo"
      broadcaster_id: "246450563"
      active: true
    - name: "jasontheween"
      broadcaster_id: "107117952"
      active: true

model:
  embed_dim: 64
  hidden_dim: 128
  max_seq_len: 512

training:
  learning_rate: 0.001
  batch_size: 32
  epochs: 20
  pos_weight: 4.0
  window_seconds: 30